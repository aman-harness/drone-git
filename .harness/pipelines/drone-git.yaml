name: Sample Pipeline_1676460287991
stages:
  - name: build
    spec:
      steps:
        - name: echo
          spec:
            run: echo hello world
          type: script
    type: ci
version: 1
